# === Env vars usadas en K8s (igual que ~/Workspace/thisuite/k8s/app/thisecure/) ===
# Los secrets reales están en app-secrets de Kubernetes

# Shared (same for all services)
JWT_SECRET=change-this-to-a-secure-hex-string
ENCRYPTION_KEY=change-this-to-a-32-byte-key

# Database
DB_HOST=192.168.1.201
DB_PORT=5432
DB_USERNAME=thsjowi
DB_PASSWORD=Th$JoWi

# Kafka
KAFKA_HOST=redpanda-bootstrap.redpanda.svc.cluster.local
KAFKA_PORT=9092

# Port por servicio (definido en deploy.yaml como containerPort, no como env var)
# notes: 8083
# otp:   8085
# password: 8084
